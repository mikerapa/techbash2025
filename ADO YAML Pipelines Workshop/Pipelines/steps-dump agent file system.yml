steps:
- script: ls -l -R $(Agent.BuildDirectory)
  displayName: list files in $(Agent.BuildDirectory) (Agent.BuildDirectory)
- script: ls -l -R $(System.ArtifactsDirectory)
  displayName: list files in $(System.ArtifactsDirectory) (System.ArtifactsDirectory)
- script: ls -l -R $(Build.SourcesDirectory)
  displayName: list files in $(Build.SourcesDirectory) (Build.SourcesDirectory)
- script: ls -l -R $(Build.BinariesDirectory)
  displayName: list files in $(Build.BinariesDirectory) (Build.BinariesDirectory)
- script: ls -l -R $(System.DefaultWorkingDirectory)
  displayName: list files in $(System.DefaultWorkingDirectory) (System.DefaultWorkingDirectory)
- script: ls -l -R $(Pipeline.Workspace)
  displayName: list files in $(Pipeline.Workspace) (Pipeline.Workspace)
- script: ls -l -R $(Build.ArtifactStagingDirectory)
  displayName: list files in $(Build.ArtifactStagingDirectory) (Build.ArtifactStagingDirectory)
